# 周期行业景气度网站 - 周度更新Prompt

每周收到新Excel底稿和PDF报告后，按以下步骤执行更新。

---

## 第一步：替换Excel底稿

将收到的新Excel文件复制到项目目录，覆盖对应旧文件：

- `煤炭.xlsx` → 煤炭数据
- `钢铁.xlsx` → 钢铁数据（注意PDF中"黑色"="钢铁"）
- `有色.xlsx` → 有色数据
- `石化.xlsx` → 石化数据
- `基础化工.xlsx` → 基础化工数据

将新PDF报告 `周期行业周度跟踪XXXXXXXX.pdf` 也放入项目目录。

---

## 第二步：重新导出图表数据和景气指数（data.js）

运行以下命令，从Excel读取最新数据并导出为 `data.js` 和 `assets/data.json`：

```bash
python export_data.py
```

这会自动完成：
- 读取5个Excel的所有sheet（价格/库存/开工/盈利/价差等）
- 计算各行业景气指数（见下方说明）
- 导出KPI卡片、图表数据、景气指数历史到 `data.js`

### 景气指数计算方法（自动，无需手动干预）

景气指数由 `index_calculator.py` 自动计算，输入数据来自Excel底稿，无需手动更新。计算流程：

1. **选取指标**：每个行业取4个核心指标（定义在 `chart_config.py` 的 `prosperity_indicators`）

| 行业 | 正向指标（高=景气） | 负向指标（低=景气） |
|------|---------------------|---------------------|
| 煤炭 | 秦港动力煤价(pct)、25省电厂日耗(pct) | 25省电厂库存(pct)、秦皇岛港库存(pct) |
| 钢铁 | 上海热卷价格(pct)、螺纹盈利(diff)、高炉开工率(diff) | 五大材库存(pct) |
| 有色 | 1#铜价格(pct)、A00铝价格(pct)、电解铝利润(diff) | 铜社会库存(pct) |
| 石化 | 布伦特原油(pct)、汽油裂解价差(diff)、山东地炼开工率(diff) | 美国商业原油库存(pct) |
| 基础化工 | 化工价格指数(pct)、POY价差(diff)、涤纶长丝开工率(diff) | PTA库存天数(pct) |

2. **同比变换**：`pct` 类指标计算同比增速（%），`diff` 类指标计算同差（绝对差值）
3. **5年滚动分位数**：当前同比值在过去260周中的百分位排名（0-100）
4. **方向调整**：负向指标（库存等）做 100-分位数 翻转
5. **PCA合成**：对4个指标的分位数做主成分分析，取第一主成分
6. **0-100映射**：将PCA得分线性映射到0-100区间，50为中性

最终输出：`currentScore`（当前景气度）、`scoreWow`（周环比变化）、`prosperityLevel`（景气/中性/不景气）

---

## 第三步：提取PDF分行业观点（summaries.js）

运行以下命令，从PDF中提取各行业标题下的原文：

```bash
python extract_summaries.py
```

这会自动完成：
- 扫描所有PDF文件，按日期倒序排列
- 识别每个PDF页面所属行业（煤炭/黑色→钢铁/有色/石化/基础化工）
- 提取行业标题下的全部文字，保留原始段落格式和换行
- 输出到 `summaries.js` 的 `summaries` 字段

**重要：分行业观点必须使用PDF原文，不可改写、缩写或添加任何内容。**

---

## 第四步：撰写市场总览（summaries.js overview字段）

这是唯一需要手动撰写的部分。根据第三步提取的各行业PDF原文，为每个行业总结**一句话**，格式如下：

```javascript
overview: {
    "YYYY-MM-DD": "本周来看，\n1）煤炭：景气度XX.X（+X.X），[一句话概括]。\n2）钢铁：景气度XX.X（+X.X），[一句话概括]。\n3）有色：景气度XX.X（+X.X），[一句话概括]。\n4）石化：景气度XX.X（+X.X），[一句话概括]。\n5）基础化工：景气度XX.X（+X.X），[一句话概括]。",
}
```

### 总览撰写规则

1. **景气度和周环比**：从 `data.js` 中读取 `currentScore` 和 `scoreWow`，不可编造数字
2. **一句话概括**：严格从该行业PDF原文中提炼核心结论，不可引入原文中没有的数据或判断
3. **与分行业观点一致**：总览中的每句话必须能在对应的PDF原文中找到依据，不可出现矛盾
4. **典型错误示例（禁止）**：
   - 总览说"铁水产量恢复至239.5万吨/日超预期"，但PDF原文说"铁水产量呈现走平态势" → 矛盾
   - 总览说"后续煤价或承压"，但PDF原文说"煤价或将面临上行压力" → 方向相反
   - 总览提到PDF原文中不存在的具体数字 → 不可引入

### 总览模板

```
本周来看，
1）煤炭：景气度[XX.X]（[+/-X.X]），[价格变化] + [供需核心矛盾]。
2）钢铁：景气度[XX.X]（[+/-X.X]），[价格变化] + [基本面核心判断]。
3）有色：景气度[XX.X]（[+/-X.X]），[价格变化] + [库存/供应核心变化]。
4）石化：景气度[XX.X]（[+/-X.X]），[价格变化] + [炼厂开工/利润核心情况]。
5）基础化工：景气度[XX.X]（[+/-X.X]），[核心品种价格变化] + [行业盈利判断]。
```

---

## 第五步：提交并推送到GitHub

```bash
git add data.js summaries.js assets/data.json
git commit -m "更新：MMDD周度数据及行业观点"
git push origin main
```

如需同时提交新Excel/PDF：

```bash
git add 煤炭.xlsx 钢铁.xlsx 有色.xlsx 石化.xlsx 基础化工.xlsx "周期行业周度跟踪XXXXXXXX.pdf"
git commit -m "更新：MMDD周度数据及行业观点"
git push origin main
```

推送后网站会自动更新（GitHub Pages）。如浏览器显示旧数据，Ctrl+Shift+R强制刷新。

---

## 检查清单

- [ ] Excel文件已替换，文件名与 `data_loader.py` 中的路径一致
- [ ] `python export_data.py` 运行成功，`data.js` 已更新
- [ ] `python extract_summaries.py` 运行成功，`summaries.js` 已更新
- [ ] 总览文字已填写，景气度和环比数值与 `data.js` 一致
- [ ] 总览中每行业一句话与PDF原文无矛盾
- [ ] 已 `git add` + `commit` + `push` 到 main 分支
- [ ] 网站可正常访问（www.cyclical-analysis.com）
